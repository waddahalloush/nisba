import 'package:get/get.dart' hide FormData;

import 'remote/api/app_api.dart';

class Repository {
  AppApi appApi = Get.find();

  //========== Post ===============
  // Future loginClient({required var data}) => appApi.loginClient(data: data);

  //========== Get ===============
}
